# Frontier Guest Check-In — Environment

# Copy to .env.local and fill values. Next.js will load it automatically.

##########################
# Core database (required)
##########################
# Single source of truth across environments. Prefer using only DATABASE_URL.
DATABASE_URL="postgresql://USER:PASS@HOST:5432/DB?schema=public"

# Optional: direct connection for Prisma migrations (skip pooling). Often same as DATABASE_URL.
DIRECT_URL="postgresql://USER:PASS@HOST:5432/DB?schema=public"

##########################
# Application URLs
##########################
# Public base URL used in emails and links. Set to your deployed host in production.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

##########################
# Security / Auth
##########################
# Strong secret for signing JWTs (auth and acceptance links). Use a long, random value.
JWT_SECRET="change-me-to-a-long-random-string"

# Optional: password to approve capacity overrides at the kiosk.
OVERRIDE_PASSWORD="change-me"

##########################
# Email (Resend)
##########################
# Needed to send emails. If omitted, the app will log warnings and skip sending.
RESEND_API_KEY=""
EMAIL_FROM="Frontier Tower <noreply@frontiertower.com>"

##########################
# Demo / Dev ergonomics
##########################
# Enable demo mode to bypass auth and use seeded users in development. NEVER enable in production.
DEMO_MODE="false"

# Increase logging noise in dev/tests when set (used by some scripts)
DEBUG=""

##########################
# Testing-only (optional)
##########################
# Prefer keeping a single DATABASE_URL. If you need an isolated DB for tests, set TEST_DATABASE_URL.
# The test harness will prefer TEST_DATABASE_URL if present, else it falls back to DATABASE_URL.
TEST_DATABASE_URL=""

# For test QR token generator (non-production). Safe to leave empty.
QR_SECRET=""

# NODE_ENV is managed by Next.js/runtime. Do not set it here.
